import { FC } from 'react';

export const GameOver: FC = () => (
  <div className="absolute inset-0 flex items-center justify-center bg-gray-900 bg-opacity-70 backdrop-blur-sm rounded-2xl text-4xl text-white uppercase">
    Game over
  </div>
);
